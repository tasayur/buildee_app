# ==============================================================
#  nginx/conf.d/proxy_params.conf
#  Flask (Gunicorn) へのリバースプロキシ共通パラメータ
# ==============================================================

proxy_http_version  1.1;
proxy_set_header    Host               $host;
proxy_set_header    X-Real-IP          $remote_addr;
proxy_set_header    X-Forwarded-For    $proxy_add_x_forwarded_for;
proxy_set_header    X-Forwarded-Proto  $scheme;      # ← Flask の enforce_https で参照
proxy_set_header    X-Forwarded-Host   $host;
proxy_set_header    X-Forwarded-Port   $server_port;
proxy_set_header    Connection         "";            # Keep-Alive for upstream

# バッファ設定 (Gunicorn との相性)
proxy_buffering         on;
proxy_buffer_size       4k;
proxy_buffers           8 16k;
proxy_busy_buffers_size 32k;

# タイムアウト (Excel出力等の重い処理に余裕を持たせる)
proxy_connect_timeout  10s;
proxy_send_timeout     60s;
proxy_read_timeout     60s;
